# Frontend environment (example)
# The backend base URL must be reachable from the browser.
# In Kavia preview, set this to the running backend URL (no trailing slash).
NEXT_PUBLIC_API_BASE_URL=https://your-backend.example

# Optional: set to "1" to log API requests to console
NEXT_PUBLIC_API_DEBUG=0
